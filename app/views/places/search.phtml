<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<?php use Phalcon\Tag; ?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(array("places/index", "Go Back")); ?></li>
            <li class="next"><?php echo $this->tag->linkTo(array("places/new", "Create ")); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Search result</h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
            <th>Name</th>
            <th>Name Of Uk</th>
            <th>Attr</th>
            <th>Parent</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $place): ?>
            <tr>
                <td><?php echo $place->id ?></td>
            <td><?php echo $place->name ?></td>
            <td><?php echo $place->name_uk ?></td>
            <td><?php echo $place->attr_id ?></td>
            <td><?php echo $place->parent_id ?></td>

                <td><?php echo $this->tag->linkTo(array("places/edit/" . $place->id, "Edit")); ?></td>
                <td><?php echo $this->tag->linkTo(array("places/delete/" . $place->id, "Delete")); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo("places/search", "First") ?></li>
                <li><?php echo $this->tag->linkTo("places/search?page=" . $page->before, "Previous") ?></li>
                <li><?php echo $this->tag->linkTo("places/search?page=" . $page->next, "Next") ?></li>
                <li><?php echo $this->tag->linkTo("places/search?page=" . $page->last, "Last") ?></li>
            </ul>
        </nav>
    </div>
</div>
